import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
// Se você tiver um arquivo CSS global, descomente a linha abaixo:
// import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
